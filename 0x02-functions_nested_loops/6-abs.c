#include "main.h"
/**
 * _abs: print the absolute value of an integer
 * @n: will be number
 *
 * Return: the absolute value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
