#include <stdio.h>

/** 
 * main - Entry point
 * Description print 0 to 9
 * Return : 0 
 */

int main (void)
{
	int num;

	for (num = 48; num <= 57; num++)
		putchar((shar)num)
	putchar('\n');
	return (0);
}
